### Leaflet Control.Scale Usage Example

Source: https://leafletjs.com/reference-2.0.0

Shows a simple example of how to instantiate and add a Control.Scale to a Leaflet map. This control displays the map's scale in both metric and imperial units.

```javascript
new Control.Scale().addTo(map);
```

--------------------------------
