### Initialize Leaflet Map and Set View

Source: https://leafletjs.com/examples/quick-start

This JavaScript code initializes a Leaflet map instance on the 'map' div and sets the initial view to specific coordinates and zoom level. It returns the map object for chaining.

```javascript
var map = L.map('map').setView([51.505, -0.09], 13);
```

--------------------------------
